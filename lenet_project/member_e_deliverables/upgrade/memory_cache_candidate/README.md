# 成员 E：片上权重缓存候选

该候选对应升级计划 P2，用于验证 D2 是否受外部权重访问限制。基线直接读取 `m_axi` 权重；候选先把 2,400 个 Conv2 权重搬入本地数组，并使用双端口 BRAM、cyclic factor=5 分割和预取循环 `PIPELINE II=1`。

## 真实 Vitis HLS 对照结果

2026-09-09 两组均完成 CSim 和 C Synthesis：

| 配置 | Latency | LUT | FF | BRAM18K | DSP | 决策 |
|---|---:|---:|---:|---:|---:|---|
| D2 外部权重基线 | 243,101 | 13,444 | 16,887 | 0 | 7 | CONTROL |
| D2 本地权重缓存 | 243,353 | 14,224 | 12,121 | 10 | 7 | STOP_NO_PERFORMANCE_GAIN |

缓存候选的延迟增加 0.1037%，LUT 增加 780，BRAM18K 增加 10；虽然 FF 减少 4,766，且缓存搬运循环成功实现 II=1，但总体没有性能收益。因此按升级计划停止，不替换主线 D2，也不继续堆叠并行度。

两组 CSim 均 PASS，最大绝对误差为 `0.0027756691`。原始报告位于 `raw_reports/`，完整 HLS 日志位于 `evidence/fresh_vitis_hls_run_20260909.txt`。

## 路径兼容说明

实际 HLS 运行在纯英文临时目录 `E:\member_e_hls_tmp_20260909`，完成后将 work、原始报告和日志复制回本交付目录。
